CREATE TABLE default.`2te116u_tmy_test_simple_rules` (`ts` Float64, `upload_id` String, `loco_num` Int32, `section` String, `POilDieselOut` Float32, `SpeedThrustMin` Float32, `PowerPolling` Float32) ENGINE = MergeTree() PARTITION BY toMonday(toDateTime(ts)) ORDER BY (loco_num, section, upload_id, ts) SETTINGS index_granularity = 8192
